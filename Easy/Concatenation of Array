class Solution {
    public int[] getConcatenation(int[] nums) {
        int[] op=new int[nums.length*2];
        for(int i=0;i<nums.length;i++){
            op[i]=nums[i];
            op[i+nums.length]=nums[i];
        }
        return op;
    }
}
