class Solution {
    public int[] buildArray(int[] nums) {
        int[] op=new int[nums.length];
        for(int i=0;i<nums.length;i++){
            op[i]=nums[nums[i]];
        }
        return op;
    }
}
